#!/usr/bin/env bash
# pre-push hook: push 前运行 pytest，失败则阻止推送
set -euo pipefail

echo "🔍 pre-push: 运行测试..."
if ! uv run pytest tests/ -q --tb=short 2>&1; then
    echo ""
    echo "❌ 测试失败，push 已阻止。请修复后重试。"
    exit 1
fi

echo "✅ 测试通过，继续 push。"
