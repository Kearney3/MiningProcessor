# docs: https://v2.tauri.app/distribute/pipelines/github/
# 构建 Tauri 桌面应用：macOS (arm64) + Windows (x64)

name: Build Tauri App

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main, 'releases/**']
  workflow_dispatch:

env:
  UV_PYTHON: "3.13"
  PYTHONUTF8: 1

# 防止同一分支多次触发
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ══════════════════════════════════════════════════════════
  # macOS arm64
  # ══════════════════════════════════════════════════════════
  build-macos:
    name: macOS arm64
    runs-on: macos-latest  # Apple Silicon (M1+)
    needs: []
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_branch || github.ref }}
          fetch-depth: 0

      - name: Setup uv
        uses: astral-sh/setup-uv@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Python sidecar (PyInstaller)
        shell: bash
        run: |
          uv sync --dev
          uv run pyinstaller tauri_bridge.spec \
            --distpath build-sidecar \
            --clean --noconfirm
          # Tauri externalBin 期望文件名带目标架构后缀
          mv build-sidecar/tauri-bridge build-sidecar/tauri-bridge-aarch64-apple-darwin
          ls -lh build-sidecar/tauri-bridge-aarch64-apple-darwin

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: "--target aarch64-apple-darwin --bundles app"

      - name: Create DMG
        shell: bash
        run: |
          BUNDLE_DIR="src-tauri/target/aarch64-apple-darwin/release/bundle"
          DMG_DIR="$BUNDLE_DIR/dmg"

          # 删除 tauri-action 可能创建的旧 DMG
          rm -f "$DMG_DIR"/*.dmg 2>/dev/null || true

          APP_DIR=$(find "$BUNDLE_DIR/macos" -name "*.app" -maxdepth 1 | head -1)
          APP_NAME=$(basename "$APP_DIR")
          VERSION=$(grep -o '"version": *"[^"]*"' src-tauri/tauri.conf.json | head -1 | cut -d'"' -f4)
          mkdir -p "$DMG_DIR"
          DMG_PATH="$DMG_DIR/${APP_NAME%.app}_${VERSION}_aarch64.dmg"

          STAGING=$(mktemp -d)
          cp -R "$APP_DIR" "$STAGING/"
          ln -s /Applications "$STAGING/Applications"
          hdiutil create \
            -volname "$APP_NAME" \
            -srcfolder "$STAGING" \
            -ov -format UDZO \
            "$DMG_PATH"
          rm -rf "$STAGING"

          echo "=== DMG verification ==="
          ls -lh "$DMG_PATH"
          hdiutil verify "$DMG_PATH" && echo "✅ DMG integrity OK" || echo "❌ DMG corrupted"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
          if-no-files-found: error

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: "矿山数据处理工具 ${{ github.ref_name }}"
          draft: true
          files: src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg

  # ══════════════════════════════════════════════════════════
  # Windows x64
  # ══════════════════════════════════════════════════════════
  build-windows-x64:
    name: Windows x64
    runs-on: windows-latest
    needs: []
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_branch || github.ref }}
          fetch-depth: 0

      - name: Setup uv
        uses: astral-sh/setup-uv@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Python sidecar (PyInstaller)
        shell: bash
        run: |
          uv sync --dev
          uv run pyinstaller tauri_bridge.spec \
            --distpath build-sidecar \
            --clean --noconfirm
          # Tauri externalBin 期望文件名带目标架构后缀
          mv build-sidecar/tauri-bridge.exe build-sidecar/tauri-bridge-x86_64-pc-windows-msvc.exe
          ls -lh build-sidecar/tauri-bridge-x86_64-pc-windows-msvc.exe

      - name: Build frontend
        run: pnpm build

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: "--target x86_64-pc-windows-msvc"

      - name: Upload Windows x64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64
          path: src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
          if-no-files-found: warn
